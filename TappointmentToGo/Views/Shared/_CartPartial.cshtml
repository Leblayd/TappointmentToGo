@using Microsoft.AspNet.Identity
@if (Request.IsAuthenticated)
{
    <ul class="nav navbar-nav navbar-right">
        <li>
            @Html.ActionLink("Cart", "About", "Home", routeValues: null, htmlAttributes: new { title = "Manage" })
        </li>
    </ul>
}
